import "./List.css"

const List = ({ data }) => {
    // NTS: when mapping, make sure to have a key attribute
    return (
        <> 
            <h1>List</h1>
            {/* learn to map a table */}
        </>
    )
}
export default List;